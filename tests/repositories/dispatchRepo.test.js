'use strict';

/**
 * tests/repositories/dispatchRepo.test.js
 * Boilerplate test suite for dispatchRepo.
 */

const dispatchRepo = require('../../src/repositories/dispatchRepo');

describe('dispatchRepo', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should be defined', () => {
    expect(dispatchRepo).toBeDefined();
  });
});
