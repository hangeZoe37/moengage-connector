'use strict';

/**
 * tests/repositories/dlrRepo.test.js
 * Boilerplate test suite for dlrRepo.
 */

const dlrRepo = require('../../src/repositories/dlrRepo');

describe('dlrRepo', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should be defined', () => {
    expect(dlrRepo).toBeDefined();
  });
});
