'use strict';

/**
 * tests/services/callbackDispatcher.test.js
 * Boilerplate test suite for callbackDispatcher.
 */

const callbackDispatcher = require('../../src/services/callbackDispatcher');

describe('callbackDispatcher', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should be defined', () => {
    expect(callbackDispatcher).toBeDefined();
  });
});
