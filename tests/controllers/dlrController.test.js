'use strict';

/**
 * tests/controllers/dlrController.test.js
 * Boilerplate test suite for dlrController.
 */

const dlrController = require('../../src/controllers/dlrController');

describe('dlrController', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should be defined', () => {
    expect(dlrController).toBeDefined();
  });
});
