'use strict';

/**
 * tests/controllers/interactionController.test.js
 * Boilerplate test suite for interactionController.
 */

const interactionController = require('../../src/controllers/interactionController');

describe('interactionController', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should be defined', () => {
    expect(interactionController).toBeDefined();
  });
});
