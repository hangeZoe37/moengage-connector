'use strict';

/**
 * tests/controllers/inboundController.test.js
 * Boilerplate test suite for inboundController.
 */

const inboundController = require('../../src/controllers/inboundController');

describe('inboundController', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should be defined', () => {
    expect(inboundController).toBeDefined();
  });
});
