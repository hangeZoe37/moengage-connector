'use strict';

/**
 * tests/mappers/interactionMapper.test.js
 * Boilerplate test suite for interactionMapper.
 */

const interactionMapper = require('../../src/mappers/interactionMapper');

describe('interactionMapper', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should be defined', () => {
    expect(interactionMapper).toBeDefined();
  });
});
